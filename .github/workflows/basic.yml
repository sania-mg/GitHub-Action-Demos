name : Basic Workflow
on: push
jobs:
  Hello-Job:
    runs-on: ubuntu-latest
    steps: 
      - name: Hello World
        run : echo "Hello World"
      - name: Multi-line bash
        run : |
            echo "I am"
            echo "a multi-line"
            echo "script"
  echo-goodbye:
      runs-on: ubuntu-slim
      steps:
        - name: Say GoodBye
          run: echo "GoodBye!..."
      
      
  
  
   
